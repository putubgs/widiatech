import { translationsEn } from './translations-en'
import { translationsId } from './translations-id'

export const translations = {
  en: translationsEn,
  id: translationsId,
}

export type TranslationType = typeof translationsEn

